version = "0.1.0"
description = "Spoofs your timezone."

aliucord {
    changelog.set(
        """
        # 0.1.0
        * dev release: Test!
        """.trimIndent(),
    )

    deploy.set(true)
}
