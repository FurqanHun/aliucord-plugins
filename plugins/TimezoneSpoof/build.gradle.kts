version = "0.2.0"
description = "Spoofs your timezone."

aliucord {
    changelog.set(
        """
        # 0.2.0
        * Rebranded to TimezoneSpoof
        * Fixed UI text box hint overlapping bug

        # 0.1.0
        * dev release: It spoofs!
        """.trimIndent(),
    )

    deploy.set(true)
}
